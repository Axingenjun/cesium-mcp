{
  "name": "cesium-mcp-bridge-test",
  "private": true,
  "version": "0.0.0",
  "description": "Browser dev playground for cesium-mcp-bridge (source HMR, no dist build)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:force": "vite --force",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cesium-mcp-bridge": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^6.0.0"
  }
}
